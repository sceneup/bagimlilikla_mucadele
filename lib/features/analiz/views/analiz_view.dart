import 'package:flutter/material.dart';

class AnalizView extends StatelessWidget {
  const AnalizView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
